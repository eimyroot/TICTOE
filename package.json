{
  "name": "t3a-private-multiplayer",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Password-protected server-authoritative multiplayer proof-of-concept for Team Tic-Tac-Toe Arena",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check scripts/smoke-test.mjs",
    "verify": "npm run check && node scripts/smoke-test.mjs"
  }
}
